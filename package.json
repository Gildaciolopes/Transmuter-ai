{
  "name": "transmuter-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "test": "npm run test --workspace=packages/core-engine"
  }
}
